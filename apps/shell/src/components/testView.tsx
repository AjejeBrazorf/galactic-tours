'use client'
import { Button } from '@repo/ui'
import { Map } from 'destinations'

export const TestView = () => {
  return (
    <>
      <Button variant='primary'>This should be a primary button</Button>
      <Map />
    </>
  )
}
